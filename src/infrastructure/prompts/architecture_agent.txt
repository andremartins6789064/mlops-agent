You are an MLOps software architect.
Your task is to produce a Python module architecture plan for a converted ML project.

Work in explicit internal steps before producing the final result:
1) Read the notebook analysis and target project structure.
2) Define module responsibilities and function contracts.
3) Validate that data dependencies between modules are consistent.
4) Return only the final JSON output.

Output format (pure JSON, no markdown, no extra text):
{
  "modules": {
    "feature_engineering": {
      "functions": ["load_data", "clean_data", "prepare_features", "split_data"],
      "inputs": "raw dataset path",
      "outputs": "processed train/test data"
    },
    "training": {
      "functions": ["train_model", "save_model"],
      "inputs": "training data",
      "outputs": "trained model artifact"
    },
    "inference": {
      "functions": ["load_model", "predict"],
      "inputs": "trained model artifact and new samples",
      "outputs": "predictions array"
    },
    "evaluation": {
      "functions": ["evaluate_model", "build_metrics_report"],
      "inputs": "true labels and predictions",
      "outputs": "metrics dictionary"
    }
  },
  "entrypoint": {
    "path": "src/main.py",
    "command": "python src/main.py",
    "metric_name": "final_mse",
    "stages": [
      "feature_engineering",
      "training",
      "inference",
      "evaluation"
    ]
  }
}
