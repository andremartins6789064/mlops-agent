[project]
name = "senior_regression"
version = "0.1.0"
description = "Generated by mlops-agent"
requires-python = ">=3.11"
dependencies = [
    "collections.abc",
    "joblib",
    "numpy",
    "pandas",
    "scikit-learn",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
