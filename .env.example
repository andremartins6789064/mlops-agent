LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=smollm2:1.7b

# Optional cloud control provider (never commit the real key).
GROQ_API_KEY=

# Optional OpenAI-compatible provider (never commit the real key).
OPENROUTER_API_KEY=

# Optional Gemini OpenAI-compatible provider (never commit the real key).
GEMINI_API_KEY=
